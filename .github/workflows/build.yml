name: Build APK

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      # 用工程自带的 wrapper 构建，版本与本地一致（gradle-wrapper.jar 已随仓库提交）
      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # 打 tag 发版前的硬校验（放在编译之前，几秒内失败，不浪费时间）：
      #   1. 签名 Secrets 缺失就直接失败。以前是静默回退 debug 签名，
      #      发出去的「正式版」装不上任何已装正式版的机器。
      #   2. tag 必须与 versionName 一致。否则应用内更新会陷入死循环：
      #      提示装完新版本，BuildConfig.VERSION_NAME 还是旧的，下次启动又提示。
      - name: Verify release prerequisites
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          missing=""
          for name in KEYSTORE_BASE64 KEYSTORE_PASSWORD KEY_ALIAS KEY_PASSWORD; do
            [ -n "${!name}" ] || missing="$missing $name"
          done
          if [ -n "$missing" ]; then
            echo "::error::tag 构建缺少签名 Secrets:$missing —— 在仓库 Settings → Secrets 补齐后重打 tag"
            exit 1
          fi

          version_name=$(grep -oP 'versionName = "\K[^"]+' app/build.gradle.kts)
          if [ "${GITHUB_REF_NAME#v}" != "$version_name" ]; then
            echo "::error::tag ${GITHUB_REF_NAME} 与 app/build.gradle.kts 的 versionName ${version_name} 不一致"
            exit 1
          fi

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: quota-board-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      # 签名 Secrets 的完整性已在上一步校验过，这里不再做存在性判断
      # （有判断就有静默回退，正是之前那个坑）
      - name: Prepare release signing
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/release.jks"
          echo "KEYSTORE_PATH=$RUNNER_TEMP/release.jks" >> "$GITHUB_ENV"

      - name: Build release APK
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload release APK
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v4
        with:
          name: quota-board-release
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error

      # 用 gh 直接创建 Release 并附加 APK：文件不存在或上传失败会直接让任务失败
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 用 CHANGELOG 里对应版本的章节作为 Release 说明（应用内更新弹窗直接读它）
          notes="$(awk -v ver="${GITHUB_REF_NAME#v}" '
            $0 ~ "^## \\[" ver "\\]" {flag=1; next}
            /^## \[/ {flag=0}
            flag {print}
          ' CHANGELOG.md)"
          if [ -n "$notes" ]; then
            gh release create "${GITHUB_REF_NAME}" \
              --title "额度台 ${GITHUB_REF_NAME}" \
              --notes "$notes" \
              app/build/outputs/apk/release/app-release.apk
          else
            gh release create "${GITHUB_REF_NAME}" \
              --title "额度台 ${GITHUB_REF_NAME}" \
              --generate-notes \
              app/build/outputs/apk/release/app-release.apk
          fi
          gh release view "${GITHUB_REF_NAME}" --json assets --jq '.assets[] | "\(.name) \(.size) bytes"'
